<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lukefire.dummyevent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditEvent"></activity>
        <activity android:name=".clubAcc" />
        <activity
            android:name=".Event"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity android:name=".studentSignup" />
        <activity android:name=".StudAccount" />
        <activity android:name=".signup" />
        <activity android:name=".clubSignup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>